# Changelog — github.com/LerianStudio/fetcher/pkg/engine

The embedded runtime engine is released as its own Go module on an independent v1
version line. Tags are path-prefixed: `pkg/engine/vX.Y.Z`. See `docs/RELEASING.md`
for the dual-module release scheme. Entries below are prepended by the release
pipeline (`scripts/release/engine-release.mjs`).
