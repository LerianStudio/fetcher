name: Fuzz Tests

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fuzz:
    uses: LerianStudio/github-actions-shared-workflows/.github/workflows/go-fuzz.yml@v1.17.0
    with:
      go_version: '1.25.7'
