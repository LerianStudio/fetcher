name: PR Validation

on:
  pull_request:
    branches: ['maintenance/v1.4.x']
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  actions: read
  contents: read
  id-token: write
  issues: write
  pull-requests: write
  security-events: write

jobs:
  validate:
    uses: LerianStudio/github-actions-shared-workflows/.github/workflows/go-pr-validation.yml@v1.36.5
    with:
      pr_title_scopes: |
        manager
        worker
        infra
        pkg
        scripts
        deps
        ci
        docs
        tests
        migrations
      go_version: "1.25.9"
      golangci_lint_version: "v2.6.2"
      app_name_prefix: "fetcher"
      filter_paths: |
        components/manager
        components/worker
      go_private_modules: "github.com/LerianStudio/*"
      run_go_analysis: false
      run_lib_version_check: false
      run_security: false
      ignore_file: ".trivyignore"
      shared_paths: |
        go.mod
        go.sum
        components/
        pkg/
        Makefile
    secrets: inherit
