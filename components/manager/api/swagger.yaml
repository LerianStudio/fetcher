basePath: /
definitions:
  Pagination:
    description: Pagination is the struct designed to store the pagination data of
      an entity list.
    properties:
      items: {}
      limit:
        example: 10
        type: integer
      page:
        example: 1
        type: integer
      total:
        example: 10
        type: integer
    type: object
  github_com_LerianStudio_fetcher_pkg_model.ConnectionInput:
    properties:
      configName:
        example: production-db
        maxLength: 100
        minLength: 3
        type: string
      databaseName:
        example: mydatabase
        type: string
      host:
        example: db.example.com
        type: string
      metadata:
        additionalProperties: {}
        type: object
      password:
        example: secretpassword
        type: string
      port:
        example: 5432
        maximum: 65535
        minimum: 1
        type: integer
      ssl:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SSLInput'
      type:
        enum:
        - ORACLE
        - SQL_SERVER
        - POSTGRESQL
        - MONGODB
        - MYSQL
        example: POSTGRESQL
        type: string
      userName:
        example: dbuser
        type: string
    required:
    - configName
    - databaseName
    - host
    - password
    - port
    - type
    - userName
    type: object
  github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse:
    properties:
      configName:
        type: string
      createdAt:
        type: string
      databaseName:
        type: string
      host:
        type: string
      id:
        type: string
      metadata:
        additionalProperties: {}
        type: object
      port:
        type: integer
      productName:
        type: string
      ssl:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SSLResponse'
      type:
        type: string
      updatedAt:
        type: string
      userName:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.ConnectionSchemaResponse:
    properties:
      configName:
        type: string
      databaseName:
        type: string
      id:
        type: string
      tables:
        items:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.TableDetails'
        type: array
      type:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.ConnectionUpdateInput:
    properties:
      configName:
        example: production-db
        maxLength: 100
        minLength: 3
        type: string
      databaseName:
        example: mydatabase
        type: string
      host:
        example: db.example.com
        type: string
      metadata:
        additionalProperties: {}
        type: object
      password:
        example: secretpassword
        type: string
      port:
        example: 5432
        maximum: 65535
        minimum: 1
        type: integer
      ssl:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SSLUpdateInput'
      type:
        enum:
        - ORACLE
        - SQL_SERVER
        - POSTGRESQL
        - MONGODB
        - MYSQL
        example: POSTGRESQL
        type: string
      userName:
        example: dbuser
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.DataRequest:
    description: DataRequest encapsulates field mappings and optional filters for
      data extraction.
    properties:
      filters:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.NestedFilters'
      mappedFields:
        additionalProperties:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
        type: object
    required:
    - mappedFields
    type: object
  github_com_LerianStudio_fetcher_pkg_model.FetcherRequest:
    description: FetcherRequest represents the request body for creating a new data
      extraction job.
    properties:
      dataRequest:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.DataRequest'
      metadata:
        additionalProperties: {}
        type: object
    required:
    - dataRequest
    type: object
  github_com_LerianStudio_fetcher_pkg_model.FetcherResponse:
    description: FetcherResponse represents the response after successfully creating
      a data extraction job.
    properties:
      createdAt:
        type: string
      jobId:
        type: string
      message:
        type: string
      status:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.JobResponse:
    description: JobResponse represents the complete information about a data extraction
      job.
    properties:
      completedAt:
        type: string
      createdAt:
        type: string
      filters:
        $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.NestedFilters'
      id:
        type: string
      mappedFields:
        additionalProperties:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
        type: object
      metadata:
        additionalProperties: {}
        type: object
      requestHash:
        type: string
      resultHmac:
        type: string
      resultPath:
        type: string
      status:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.NestedFilters:
    additionalProperties:
      additionalProperties:
        additionalProperties:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model_job.FilterCondition'
        type: object
      type: object
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SSLInput:
    properties:
      ca:
        example: |-
          -----BEGIN CERTIFICATE-----
          ...
        type: string
      cert:
        type: string
      key:
        type: string
      mode:
        example: require
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SSLResponse:
    properties:
      mode:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SSLUpdateInput:
    properties:
      ca:
        example: |-
          -----BEGIN CERTIFICATE-----
          ...
        type: string
      cert:
        type: string
      key:
        type: string
      mode:
        example: require
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SchemaValidationError:
    properties:
      dataSourceId:
        type: string
      field:
        type: string
      table:
        type: string
      type:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SchemaValidationErrorResponse:
    properties:
      code:
        type: string
      errors:
        items:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SchemaValidationError'
        type: array
      message:
        type: string
      title:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SchemaValidationRequest:
    description: Request body for schema validation containing mapped fields per datasource.
    properties:
      mappedFields:
        additionalProperties:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
        description: |-
          MappedFields maps datasource config names to their tables and fields
          Key: configName (e.g., "midaz_onboarding")
          Value: map of table names to field names
        type: object
    required:
    - mappedFields
    type: object
  github_com_LerianStudio_fetcher_pkg_model.SchemaValidationResponse:
    properties:
      errors:
        items:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SchemaValidationError'
        type: array
      message:
        type: string
      status:
        description: '"success" or "failure"'
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model.TableDetails:
    properties:
      fields:
        items:
          type: string
        type: array
      name:
        type: string
    type: object
  github_com_LerianStudio_fetcher_pkg_model_job.FilterCondition:
    properties:
      between:
        description: |-
          Between specifies a range condition with exactly two values [min, max].
          Matches records where min <= field <= max
          Example: {"between": [100, 1000]} matches records where 100 <= field <= 1000
        items: {}
        type: array
      eq:
        description: |-
          Equals specifies exact value matches. Multiple values treated as OR conditions.
          Example: {"eq": ["active", "pending"]} matches records where field equals "active" OR "pending"
        items: {}
        type: array
      gt:
        description: |-
          GreaterThan specifies values that must be greater than the provided value.
          Should contain exactly one value for comparison.
          Example: {"gt": [100]} matches records where field > 100
        items: {}
        type: array
      gte:
        description: |-
          GreaterOrEqual specifies values that must be greater than or equal to the provided value.
          Should contain exactly one value for comparison.
          Example: {"gte": ["2025-06-01"]} matches records where field >= "2025-06-01"
        items: {}
        type: array
      in:
        description: |-
          In specifies a list of values where the field must match any one of them.
          Multiple values treated as OR conditions.
          Example: {"in": ["active", "pending", "suspended"]} matches any of these statuses
        items: {}
        type: array
      like:
        description: |-
          Like specifies values that must match the provided value using LIKE pattern matching.
          Should contain exactly one value for comparison.
          Example: {"like": ["%active%"]} matches any status containing "active"
        items: {}
        type: array
      lt:
        description: |-
          LessThan specifies values that must be less than the provided value.
          Should contain exactly one value for comparison.
          Example: {"lt": [1000]} matches records where field < 1000
        items: {}
        type: array
      lte:
        description: |-
          LessOrEqual specifies values that must be less than or equal to the provided value.
          Should contain exactly one value for comparison.
          Example: {"lte": ["2025-06-30"]} matches records where field <= "2025-06-30"
        items: {}
        type: array
      ne:
        description: |-
          NotEquals specifies values that must NOT match the provided value.
          Should contain exactly one value for comparison.
          Example: {"ne": ["active"]} excludes this status
        items: {}
        type: array
      nin:
        description: |-
          NotIn specifies a list of values where the field must NOT match any of them.
          Multiple values treated as AND NOT conditions.
          Example: {"nin": ["deleted", "archived"]} excludes these statuses
        items: {}
        type: array
    type: object
  pkg.HTTPError:
    properties:
      code:
        type: string
      entityType:
        type: string
      err: {}
      message:
        type: string
      title:
        type: string
    type: object
host: localhost:4006
info:
  contact: {}
  description: API documentation for the Fetcher Manager component
  termsOfService: http://swagger.io/terms/
  title: Fetcher Manager API
  version: 1.0.0
paths:
  /v1/fetcher:
    post:
      consumes:
      - application/json
      description: Create a new data extraction job. The request will be validated,
        deduplicated within a 5-minute window, and all referenced connections will
        be tested before job creation. The metadata.source field is required for product
        isolation and datasource ownership validation.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Fetcher request payload. metadata.source is required.
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.FetcherRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Duplicate request - returning existing job
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.FetcherResponse'
        "202":
          description: Job created and queued for processing
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.FetcherResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "409":
          description: Conflict
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "413":
          description: Request Entity Too Large
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Create fetcher job
      tags:
      - Fetcher
  /v1/fetcher/{id}:
    get:
      description: Retrieve detailed information about a specific data extraction
        job.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Job ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.JobResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Get fetcher job
      tags:
      - Fetcher
  /v1/management/connections:
    get:
      description: List connections with pagination and filters. When X-Product-Name
        is provided, returns only connections associated with that product.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Product name. When provided, filters connections by product.
        in: header
        name: X-Product-Name
        type: string
      - default: 1
        description: Page number (minimum 1)
        in: query
        name: page
        type: integer
      - default: 50
        description: Page size (default 50, max 1000)
        in: query
        name: limit
        type: integer
      - default: desc
        description: Sort order
        enum:
        - asc
        - desc
        in: query
        name: sortOrder
        type: string
      - description: Filter by database type
        enum:
        - ORACLE
        - SQL_SERVER
        - POSTGRESQL
        - MONGODB
        - MYSQL
        in: query
        name: type
        type: string
      - description: Filter by host
        in: query
        name: host
        type: string
      - description: Filter by database name
        in: query
        name: databaseName
        type: string
      - description: Filter by start date (YYYY-MM-DD)
        in: query
        name: startDate
        type: string
      - description: Filter by end date (YYYY-MM-DD)
        in: query
        name: endDate
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/Pagination'
            - properties:
                items:
                  items:
                    $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse'
                  type: array
                limit:
                  type: integer
                page:
                  type: integer
                total:
                  type: integer
              type: object
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: List connections
      tags:
      - Connections
    post:
      consumes:
      - application/json
      description: Create a new database connection for the organization. The X-Product-Name
        header is required and identifies the product for this connection. ConfigName
        must be unique per organization (and per product when assigned); password
        is encrypted and a UUID is generated on creation.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Product name (required, non-empty)
        in: header
        name: X-Product-Name
        required: true
        type: string
      - description: Connection payload
        in: body
        name: connection
        required: true
        schema:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionInput'
      produces:
      - application/json
      responses:
        "201":
          description: Created connection identifier
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "409":
          description: Conflict
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Create connection
      tags:
      - Connections
  /v1/management/connections/{id}:
    delete:
      description: Soft delete a connection when no active jobs are running for it.
        Returns 409 if there is any active job.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "204":
          description: No Content
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "409":
          description: Conflict
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Delete connection
      tags:
      - Connections
    get:
      description: Get connection details by ID for the given organization.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Get connection
      tags:
      - Connections
    patch:
      consumes:
      - application/json
      description: Apply a partial update to a connection. Only include fields you
        want to change. Returns 409 if there is any active job.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to update (only include fields you want to change)
        in: body
        name: connection
        required: true
        schema:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionUpdateInput'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "409":
          description: Conflict
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Update connection
      tags:
      - Connections
  /v1/management/connections/{id}/assign:
    post:
      description: Associate an unassigned connection to a product. This is a one-time,
        irreversible operation for migration purposes. The product name must be provided
        via the X-Product-Name header.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      - description: Product name to assign
        in: header
        name: X-Product-Name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "409":
          description: Conflict
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Assign connection to product
      tags:
      - Migration
  /v1/management/connections/{id}/schema:
    get:
      description: Get the database schema (tables and fields) for a connection.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionSchemaResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Get connection schema
      tags:
      - Connections
  /v1/management/connections/{id}/test:
    post:
      description: Test the configured connection by establishing and closing a connection.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Connection ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Connection test result
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "429":
          description: Too Many Requests
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Test connection
      tags:
      - Connections
  /v1/management/connections/unassigned:
    get:
      description: List connections that have no product assigned, useful for migration
        purposes.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - default: 1
        description: Page number (minimum 1)
        in: query
        name: page
        type: integer
      - default: 50
        description: Page size (default 50, max 1000)
        in: query
        name: limit
        type: integer
      - default: desc
        description: Sort order
        enum:
        - asc
        - desc
        in: query
        name: sortOrder
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/Pagination'
            - properties:
                items:
                  items:
                    $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.ConnectionResponse'
                  type: array
                limit:
                  type: integer
                page:
                  type: integer
                total:
                  type: integer
              type: object
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: List unassigned connections
      tags:
      - Migration
  /v1/management/connections/validate-schema:
    post:
      consumes:
      - application/json
      description: Validate that tables and fields referenced in the request exist
        in the configured datasources. Returns 200 when validation passes, 422 when
        validation fails with detailed error information.
      parameters:
      - description: The authorization token in the 'Bearer access_token' format.
          Only required when auth plugin is enabled.
        in: header
        name: Authorization
        type: string
      - description: Schema validation request
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SchemaValidationRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Validation successful - all tables and fields exist
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SchemaValidationResponse'
        "400":
          description: Invalid request payload or missing headers
          schema:
            $ref: '#/definitions/pkg.HTTPError'
        "422":
          description: Validation failed - schema errors found (missing tables, fields,
            or unreachable datasources)
          schema:
            $ref: '#/definitions/github_com_LerianStudio_fetcher_pkg_model.SchemaValidationErrorResponse'
        "500":
          description: Internal server error
          schema:
            $ref: '#/definitions/pkg.HTTPError'
      summary: Validate schema
      tags:
      - Connections
swagger: "2.0"
